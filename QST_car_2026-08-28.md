2026-08-28 工作日志

一、Hi3861 OLED显示

1. 实现了基于I2C协议的SSD1306 OLED屏幕驱动，在Hi3861开发板上完成"QST CAR"字样及实时时钟（时:分:秒）的显示。代码通过CMSIS-RTOS创建独立线程Task1，在while循环中每秒刷新一次时间，SSD1306_ShowStr函数负责将字符串渲染到OLED指定行。

2. 在基础显示之上进一步拓展，将OLED显示内容改为汉字"鸿蒙先锋号"。由于SSD1306不带中文字库，通过取模工具生成120×24像素的bitmap字模数组`hongmeng_bmp[]`，使用SSD1306_DrawBMP函数将位图直接绘制到屏幕（page 2-4, x=4~123），实现了自定义汉字的显示效果。

该过程中学习了OLED的I2C通信原理和SSD1306的页寻址模式，理解了点阵字体与位图取模的基本方法。取模参数（字体、大小、扫描方式）的配置在AI大模型的辅助下完成。

二、Hi3861 温湿度测量

1. 完成了SHT20温湿度传感器的驱动与数据读取。使用hal_bsp_sht20.h库，通过I2C接口与SHT20通信，调用SHT20_Init()初始化传感器，SHT20_ReadData()读取温度和湿度浮点值并通过串口打印。

2. 在代码架构上采用了多线程+信号量同步的设计：创建三个线程，thread1每3秒释放两次信号量sem1，thread2和thread3各自等待获取信号量后执行——thread2负责读取并打印温湿度数据，thread3作为同步辅助线程。这种设计保证了传感器读取的周期性以及多任务的协调运行。

该过程中理解了CMSIS-RTOS信号量（Semaphore）的用法：osSemaphoreNew创建信号量（初始值0，最大值4），osSemaphoreRelease释放，osSemaphoreAcquire获取。信号量初始为0使得thread2/thread3启动后立即阻塞，必须等thread1释放后才能运行，形成"生产者-消费者"同步模式。

三、Hi3861 蓝牙收发信息

1. 实现了Hi3861的UART蓝牙串口收发功能。配置GPIO_0/UART1_TXD和GPIO_1/UART1_RXD复用为UART1，波特率9600、8数据位、1停止位、无校验。通过UartWrite发送"Hello, QST!"字符串，UartRead接收蓝牙端发来的数据。

2. 在多线程架构上采用消息队列（Message Queue）实现线程间数据传递：UART_Task负责串口发送与消息队列接收，thread2负责串口接收并将数据通过osMessageQueuePut写入消息队列，UART_Task再通过osMessageQueueGet取出并打印，形成完整的收发闭环。thread3作为监控线程定期打印运行状态。

该过程中学习了CMSIS-RTOS消息队列的创建与使用：osMessageQueueNew(MSGQUEUE_OBJECTS, 100, NULL)创建容量16、每条消息100字节的队列，osMessageQueuePut/Get用于线程间安全地传递数据。同时定义了MSGQUEUE_OBJ_t结构体封装消息缓冲区指针和索引，使数据传递更加规范。

待验证：SHT20温湿度读数精度、蓝牙收发稳定性及通信距离。
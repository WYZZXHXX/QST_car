2026-08-31 工作日志

一、Hi3861 AP3216C 传感器 + OLED 同步显示 + 光照控灯

1. 完成 AP3216C 三合一传感器（IR / ALS / PS）读数在 OLED 屏幕上的同步显示。新增 DisplaySensorData() 将数值格式化后通过 SSD1306_ShowStr() 渲染到 OLED，串口打印与 OLED 刷新同步，每秒更新。

2. OLED 显示优化：字体从 8px 改为 16px，四行填满 64px 全屏——标题居中（x=8）、数据左缩进（x=16），视觉清晰度大幅提升。两个 I2C 设备初始化之间增加 10ms 延时。

3. 新增光照感应自动控灯：ALS 低于阈值（100）时 GPIO_6 板载 LED 点亮，高于阈值时熄灭。LedInit() 不调用 GpioInit()（由 AP3216C_Init 统一调用），避免 I2C 总线复位。OLED 标题行同步显示 LED:ON/OFF。

4. 排查并解决了添加 SSD1306 驱动后系统卡死的问题：根因为 BUILD.gn 跨目录引用导致 GN 构建缓存异常，改为将 SSD1306 驱动文件本地化到项目目录内，配合 rm -rf out/ 清缓存后恢复。

5. 该过程中深入理解了 GpioInit() 的全局复位特性——在 I2C 已初始化后再次调用 GpioInit() 会将 GPIO_9/10 的 I2C 功能重置为默认输入，导致整个 I2C 总线挂死。解决方案是将 GpioInit() 统一放在初始化序列最前面，后续模块只做 IoSetFunc + GpioSetDir。

二、Hi3861 舵机转向控制

1. 完成 SG90 舵机驱动，通过 GPIO_2 输出 20ms 周期 PWM（0.5~2.5ms 高电平对应 0°~180°），封装了 0°/45°/90°/135°/180° 五个角度函数。

2. 采用三线程 + 互斥锁架构：thread1 获取互斥锁后转 90° 并持有 5 秒，thread3 获取互斥锁后转 180° 并持有 3 秒，thread2 监控标志位并打印当前角度。互斥锁保证同一时刻只有一个线程控制舵机。

三、Hi3861 TCRT5000 红外循迹

1. 完成 TCRT5000 红外传感器驱动，GPIO_13/GPIO_14 分别对应左右探头，配置为输入模式，通过 GpioGetInputVal() 读取电平——低电平表示检测到黑线，高电平表示白底。

2. 采用 CMSIS-RTOS 定时器实现周期性检测：osTimerNew 创建周期定时器（50ms），回调函数 Timer1_Callback 中循环执行传感器读取，串口输出 left/right black/white 状态。

四、工具链环境适配

1. 排查了 DSH 无法直接写入 SSHFS 映射盘（Z: / Y:）的问题。通过 PowerShell 调用 [System.IO.File]::WriteAllText() 绕过该限制。

2. 验证了 VMware NAT + WinFsp + SSHFS-Win 连接架构，确认文件读写权限正常。

五、阶段小结

至此 Hi3861 端已完成的模块：OLED 显示（含中文字库取模）、SHT20 温湿度（信号量同步）、UART 蓝牙收发（消息队列）、HC-SR04 超声波测距、SG90 舵机转向（互斥锁）、TCRT5000 红外循迹（定时器）、AP3216C 三合一传感器（含 OLED 同步显示与光照控灯）。累计覆盖了 CMSIS-RTOS 的线程、信号量、互斥锁、消息队列、定时器五类 IPC 机制，以及 GPIO、I2C、UART 三类外设总线。

已完成：AP3216C + OLED 16px 大字显示正常，传感器数据每秒刷新；LED 自动控灯验证通过，手遮传感器灯亮、松开灯灭。后续可微调 ALS 暗光阈值以适配实际环境。
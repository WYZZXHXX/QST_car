2026-09-01 工作日志

一、Hi3861 → STM32 串口通信协议（电机控制）

1. 定义并实现了 Hi3861 与 STM32 之间的电机控制通信协议。stm32\_motor\_control() 函数按 6 字节帧格式通过 UART2 下发控制指令：帧头 0xFC、左轮方向、左轮速度、右轮方向、右轮速度、帧尾 0xFD，波特率 115200。方向编码为 0 正转 / 1 反转，速度幅值统一限制在 ±150 内。
2. 在协议基础上封装了五个运动函数：car\_forward() 前进（100,100）、car\_backward() 后退（-100,-100）、car\_left() 左转（50,150）、car\_right() 右转（150,50）、car\_stop() 停止（0,0），通过左右轮差速实现转向。
3. UART2 通信配置：GPIO\_11 复用为 UART2\_TXD、GPIO\_12 复用为 UART2\_RXD，波特率 115200、8 数据位、1 停止位、无校验。GpioInit() 统一置于初始化序列最前，随后仅做 IoSetFunc 复用，避免总线被重置。
4. 采用双线程 + 互斥锁架构验证协议：thread1 获取互斥锁后调用 car\_forward() 前进 1 秒，thread2 获取互斥锁后调用 car\_left() 左转 1 秒，osMutexNew 创建互斥锁保证同一时刻只有一个线程控制电机。

二、STM32 电机执行端（串口接收指令）

1. 完成 STM32 端主程序，用于接收 Hi3861 下发的电机指令：初始化 72MHz 系统时钟、NVIC 中断分组、USART1（115200，调试 printf）、USART2（115200，接收电机指令）、编码器 TIM2/TIM3、PWM TIM4（1kHz）、RGB LED 及 1ms SysTick。主循环仅 delay\_ms(20) 空闲，电机动作由 USART2 中断驱动。
2. 针对接线方案编写了两个版本：串口通信.txt 与小车边缘自动转向.txt 均采用 USART2（PA3）接收电机指令、USART1 发送 printf，电机执行由 USART2 中断驱动，与 Hi3861 端 UART2（GPIO\_11/12）保持一致。

三、Hi3861 边缘自动转向（TCRT5000）

1. 补全了 Hi3861/小车边缘自动转向.txt：基于 TCRT5000 红外对管实现边缘检测自动转向。GPIO\_13 接左红外、GPIO\_14 接右红外，通过 EDGE\_IS\_LOW 宏配置触发极性（0=高电平为边缘、1=低电平为边缘），便于适配不同模块的输出极性。
2. 采用 2ms 周期软件定时器（osTimerPeriodic）驱动四状态状态机：

   * FWD（前进 60）：检测到边缘后经 60ms 消抖（连续 3 次采样）确认触发，并锁存触发侧 edge\_side；
   * BACK（后退 50）：后退 400ms 后依据触发侧决定转向方向；
   * TURN\_L / TURN\_R（原地左/右转 ±60）：转向 600ms 后恢复前进。
转向逻辑为"远离边缘"：左传感器触发则右转，右传感器触发则左转。
3. 复用 6 字节电机控制协议，通过 UART2（GPIO\_11 TXD / GPIO\_12 RXD，115200）向 STM32 下发指令，速度统一钳位 ±150。每 500ms 打印一次 \[TCRT] 传感器原始值、边缘标志与当前状态，便于上电调试。

四、阶段小结

至此 Hi3861 端在完成传感器采集、舵机转向、红外循迹、超声波测距、蓝牙收发等模块后，今天打通了 Hi3861 与 STM32 之间的 UART 电机控制链路，并在此协议上落地了 TCRT5000 边缘自动转向应用，形成"鸿蒙端决策、STM32 端执行"的分工：Hi3861 负责上层逻辑并下发 6 字节运动指令，STM32 负责接收指令并驱动电机、编码器闭环。

待验证：Hi3861 与 STM32 实际联调，确认串口指令收发正确、电机动作与预期一致。。


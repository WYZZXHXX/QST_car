2026-09-05 工作日志

一、循迹小车基础循迹优化（15.0_Line_Track）

1. 延续昨日岔路口循迹方案，继续调试 Hi3861 端 `15.0_Line_Track/line_track.c`。当前基础逻辑为：TCRT5000 左=GPIO13、右=GPIO14，高电平表示压到黑线；双白时直行，单边压线时采用两轮差速弧线修正，双黑时进入岔口转向。

2. 保留岔口交替策略：第一次有效双黑执行左转，第二次执行右转，之后单双交替。岔口转弯仍使用两轮同向、内侧慢外侧快的弧线方式，不修改当前已经基本正常的双黑转向参数：`JUNCTION_IN_SPEED=35`、`JUNCTION_OUT_SPEED=60`、`JUNCTION_TURN_TICKS=60`、`JUNCTION_EXIT_TICKS=8`。

3. 普通循迹保留单边压线后的持续修正机制：左/右单边压线时记录 `last_correction` 并装载 `CORRECTION_HOLD_TICKS=3`；随后短暂检测到双白时继续保持上一次修正方向，避免传感器刚离开黑线就立即恢复直行，改善车辆贴线行驶的问题。

二、编译问题排查与修复

1. 远程构建时报错 `missing terminating " character`，定位到 `line_track.c` 中启动日志、传感器状态日志以及左右岔口日志的字符串被文本处理破坏，实际换行嵌入了 C 字符串，导致编译器认为字符串没有结束，同时连带产生大量函数/变量未使用错误。

2. 将以下 4 处日志统一修复为 C 字符串中的 `\\r\\n` 转义序列：
   - `printf("start line tracking\\r\\n");`
   - `printf("L%d R%d\\r\\n", (int)l, (int)r);`
   - `printf("junction %d: left\\r\\n", junction_count);`
   - `printf("junction %d: right\\r\\n", junction_count);`

3. 远程构建日志显示工程已成功生成 57 个目标，但在编译 `15.0_Line_Track/line_track.c` 时因上述字符串问题中止。修复后本地挂载环境再次执行构建时受到 Python/路径权限问题影响，未能在本机取得完整的最终构建结果；源码中 4 处字符串已按字节确认使用反斜杠转义，而不是实际换行。

三、左右转向方向复核

1. 实车反馈：将普通循迹左右电机输出交换后，左传感器单独压线时车辆反而向左调整，说明原有普通循迹方向映射是正确的。因此恢复原始映射：
   - `car_left()`：`motor_send(TURN_IN_SPEED, TURN_OUT_SPEED)`；
   - `car_right()`：`motor_send(TURN_OUT_SPEED, TURN_IN_SPEED)`。

2. 岔口转向也恢复与普通循迹一致的基础方向：
   - `turn_left_at_junction()`：`motor_send(JUNCTION_IN_SPEED, JUNCTION_OUT_SPEED)`；
   - `turn_right_at_junction()`：`motor_send(JUNCTION_OUT_SPEED, JUNCTION_IN_SPEED)`。

3. 结合主循环确认，第一次双黑时 `junction_count` 从 0 变为 1，执行 `junction_count % 2` 的左转分支；转向期间 `junction_armed=0`，只有重新检测到双白后才重新布防，不会把同一次双黑重复判断为第二次。

四、岔口响应速度优化

1. 为排除双黑连续确认造成的响应延迟，将：
   - `JUNCTION_CONFIRM_TICKS`：`4` → `1`。

2. 修改后首次检测到 `l && r` 即进入岔口处理并执行转向，同时保留 `junction_armed` 防重复机制。双黑部分实车表现基本正确，后续不再调整该部分。

五、单边压线修正角度调整

1. 根据实车反馈，仅增大普通单边压线时的调整角度，不改动双黑岔口转向部分。

2. 将普通循迹内侧轮速度由 `25` 降至 `20`，外侧轮速度保持 `60`：
   - `TURN_IN_SPEED=20`；
   - `TURN_OUT_SPEED=60`。

3. 这样普通单边修正的左右轮速度差由 `35` 增加到 `40`，车辆在单边压线时会进行更明显的弧线调整；`JUNCTION_IN_SPEED/JUNCTION_OUT_SPEED` 等双黑参数保持原值不变。

六、阶段小结

今日主要完成了 15.0 循迹小车的源码整理、编译错误定位、左右转向方向复核、双黑立即响应以及普通单边压线角度优化。确认普通循迹原始左右映射正确，双黑不会因转弯耗时被重复判断；将双黑确认次数降为 1 后，首次检测双黑即可进入岔口转向；在不改变双黑岔口参数的前提下，将普通循迹内侧轮速度从 25 调至 20，使单边压线修正角度稍微增大。

待验证：重新同步最新 `line_track.c` 后完成远程构建并烧录，重点观察单边压线时车辆是否能更快回到黑线中心，同时确认双黑岔口仍保持当前正确的左右转向和转弯幅度。

2026-09-04 工作日志

一、排查循迹车「检测到黑线无反应」问题

1. 现象：小车能正常走直线、串口能打印到黑线（L/R 值变化），但检测到黑线时电机无动作、不转向。

2. 定位 STM32 固件过期：比对时间戳发现 STM32 源码（`main.c`/`uart.c`，16:50）晚于固件 `Template.hex`（16:18），最新转向逻辑未烧录进芯片；Hi3861 侧 `line_track.c`（16:40）早于 `Hi3861_wifiiot_app_burn.bin`（16:53），鸿蒙端为最新版。即「检测端新、执行端旧」，检测到黑线后 STM32 端无对应动作。

3. 编译报错 `main.c(49): error #268: declaration may not appear after executable statement`：`static uint8_t prev_led` 写在 `printf` 等函数调用之后，Keil ARMCC V5 默认 C90 标准不允许变量声明出现在可执行语句之后。将声明移到 `main()` 最前解决，重新编译烧录后小车恢复「基本正常」。

二、岔路口转弯优化（方案 B：确定性转向 + 左手法则）

1. 需求：小车沿黑线（线在车身正中地面）行驶，在约 Y 字型岔口找对正确路径；死路尽头为 T 字型黑线，走到死路需倒回上一个岔口并换一个方向。

2. 方案 B 设计思路：把原「转到有线为止」的 `spin_left_until_line` 改为确定性转向状态机。利用岔口几何差异——Y 岔口左右分支约在来车方向 ±60°、T 死路横杠约 ±90°——原地转 60° 能压到 Y 分支、却压不到 T 横杠，据此区分「岔口」与「死路」，避免把来路误判成分支。

3. 左手法则优先级：左(转 60°) → 右(转 120°) → 调头(转 180°)。死路直接原地 180° 调头、沿来路退回，替代原 `reverse_to_junction` 的盲倒车（无传感器引导、不校验是否回到岔口）。

4. STM32 改为纯执行器：移除 `uart.c` 的 `g_line_event` 路口拦截逻辑（原逻辑在收到停车/转向帧时自行停 300ms、丢帧、再调整，会与 Hi3861 的确定性转向指令相互冲突），`main.c` 同步移除路口停顿块。循迹与路口决策全部收敛到 Hi3861 端。

三、调试中解决的问题

1. 编译报错 `car_back defined but not used`（工程开启 `-Werror=unused-function`）：方案 B 用原地 180° 调头替代直线倒车后，`car_back()` 不再被调用，删除该未使用函数解决。

2. STM32 `motor.c` 停车 bug（关键）：原 `Set_Pwm` 的 `else` 分支为 `PWMA = 7199 - myabs(motoA)`，当 `motoA == 0`（停车）时算出 `7199 - 0 = 7199` 满占空比并置反转方向，等于「停车变全速倒车」。此前直线能走只因直行帧两轮均为正值绕过了该分支。改为三分支（`>0` 正转、`<0` 反转、`==0` 停止且占空比清零）后，停车才是真停。

3. TCRT5000 极性复核：实车打印确认本模块为「高电平=压线」，`TcrtRead` 以 `v == VALUE1` 判黑，与 09-03 结论一致。

四、阶段小结

今日排查并修复了循迹车「检测到黑线无反应」问题（根因：STM32 固件过期 + C90 变量声明位置编译报错），并针对岔路口转弯设计了方案 B（确定性 60° 转向 + 左手法则），期间发现并修复了 STM32 `motor.c` 的停车 bug（0 速度误判为反转满油门）。方案 B 实车联调中仍存在「白桌面读 L0 R0 但车乱动、亮红灯」的现象，已按需回退到「基本正常」版本（`line_track.c`/`uart.c`/`main.c`/`uart.h` 恢复原版），保留 `motor.c` 停车修复。

待验证：方案 B 岔路口转弯需重新、逐步推进——先把「单侧压线转弯方向」「传感器极性基准打印」一次钉死，再叠加确定性转向，避免多变量同时改动导致无法定位。

2026-09-02 工作日志

一、Hi3861 蓝牙遥控小车（BLE → 电机控制）

1. 完成手机蓝牙遥控小车 `13.0_BLE_Car`：手机「蓝牙调试器」App 通过蓝牙发送指令（前进/后退/左转/右转/停止），蓝牙模块接 Hi3861 `UART1`（GPIO0=TX / GPIO1=RX）把指令透传给 Hi3861，Hi3861 解析后经 GPIO11 软件模拟串口 TX（bit-bang，9600）下发 6 字节电机控制帧给 STM32F103 的 USART1，由 STM32 驱动 L9110S 电机。

2. 说明为何用 GPIO11 bit-bang 而非 UART2：Hi3861 的 UART1 与 UART2 不能同时初始化，而电机链路是单向只发，所以用 GPIO11 软件模拟串口发送，省掉 UART2。与之对应，STM32 端 `Uart1_Init` 改为 9600 波特率，与本文件 `MOTOR_BAUD` 保持一致。

3. 手机端指令表（字母与数字两种按键面板均支持）：`F/f/W/w/1` 前进、`B/b/2` 后退、`L/l/3` 左转、`R/r/4` 右转、`S/s/0` 停止；均为持续动作，直到下发新指令才切换。

4. 复用 6 字节电机控制帧（Hi3861 → STM32，9600）：`0xFC + A_dir + motorA + B_dir + motorB + 0xFD`，方向 0=正转（前进）/1=反转（后退），速度幅值统一限制在 0~150。

5. 运动参数：前进速度 60、后退速度 50、转弯内侧轮 25、转弯外侧轮 55，并预留 `FWD_LEFT_TRIM` 直行微调（车往右偏增大、往左偏减小，范围约 ±15）。转弯采用左右轮差速实现边走边转。

6. 软件串口实现：`motor_uart_putc` 按起始位 + 8 数据位（LSB 先）+ 停止位逐 bit 翻转 GPIO11，位宽 104us（1000000/9600）；`motor_send` 在发送期间用 `hi_int_lock`/`hi_int_restore` 关中断保证时序，避免 STM32 收到乱码帧。

7. 蓝牙接收任务 `BleCarTask`：`UART1` 非阻塞读、10ms 轮询，逐字节送入 `parse_cmd` 解析；上电先执行 `car_stop()` 停车。

二、STM32 电机执行端（USART1 接收，9600）

1. 完成 STM32 端主程序：初始化 72MHz 系统时钟、NVIC 中断分组（2）、USART1（9600，PA10 接收电机指令 / PA9 发送 printf）、关闭 JTAG 并开启 SWD、编码器 TIM2（左，PA0/PA1）与 TIM3（右，PA6/PA7）、PWM TIM4（1kHz，7199 分频/9 周期）、RGB LED。主循环仅 `delay_ms(20)` 空闲，电机动作由 USART1 中断驱动。

2. 接线/波特率调整：相比昨日 USART2（115200）方案，本次电机指令改由 USART1（PA10）接收、9600 波特率，与 Hi3861 端 GPIO11 软串口 TX 对应，形成「Hi3861 GPIO11 → STM32 PA10」的单向电机链路。

三、Hi3861 WiFi STA 连接手机热点

1. 完成小车连接手机热点模块（`10.0_Integrated/wifi_connect.c`）：基于 OpenHarmony 系统 wifiservice 服务 API，让 Hi3861 以 STA（Station）模式连接手机热点，连接成功后通过 DHCP 自动获取 IP，并每 3s 周期打印连接状态与 IP 地址，供后续 TCP/UDP 等网络通信使用。

2. 参考 `vendor/.../netcfg_sample.c` 与 `wifiservice_func_test.c` 实现，主要流程：`WifiStaStart`（EnableWifi → IsWifiActive → RegisterWifiEvent → netif_find wlan0）、`WifiStaConnect`（AddDeviceConfig → ConnectTo）、`WifiPrintStatus`（GetLinkedInfo → ip4addr_ntoa 打印 IP）。

3. 连接状态回调 `OnWifiConnectionChanged`：在 `WIFI_STATE_AVALIABLE` 时置连接标志并 `netifapi_dhcp_start` 启动 DHCP 自动获取 IP，断开时清标志并打印 `disconnectedReason`。热点信息通过 `WIFI_SSID` / `WIFI_PSK` 宏配置，密码为空时按开放热点 `WIFI_SEC_TYPE_OPEN` 处理，否则按 `WIFI_SEC_TYPE_PSK`（WPA2-PSK）。

4. 编译注意：本文件编译需在 BUILD.gn 的 include_dirs 中加入 `//foundation/communication/interfaces/kits/wifi_lite/wifiservice` 与 `//vendor/hisi/hi3861/hi3861/third_party/lwip_sack/include` 两个头文件路径。

四、阶段小结

今日在昨日打通 Hi3861 ↔ STM32 串口链路的基础上，落地了手机蓝牙遥控整车链路：手机 App → 蓝牙模块（UART1 透传）→ Hi3861 解析 → GPIO11 软串口（bit-bang，9600）→ STM32 USART1 中断驱动电机。同时新增 Hi3861 WiFi STA 连接手机热点模块，为后续 TCP/UDP 网络遥控做铺垫。分工上 Hi3861 负责通信接入与上层决策、STM32 负责电机执行与编码器闭环。

2026-08-27 工作日志
一、STM32方面
1. 完成了编码器驱动的编写，新建Encoder.h头文件，实现TIM2/TIM3编码器模式下的脉冲读取与清零功能，并整合到control_system.h中。
2. 排查并修复了电机方向与闭环映射问题：两轮转向相反导致无法走直线。经分析发现是目标速度符号、Set_Pwm参数映射交叉、以及调用处交叉闭环三重原因叠加，逐一修正后小车可直线行驶。同时补全了PID的积分项。
3. 在control_system.c中新增位置控制功能，通过状态机实现前进1米→刹车→后退1米→停止的自动循环，控制频率提升至50Hz。轮径和速度参数仍需实测微调。
4. 新建nfc.c/nfc.h，完成PN532 NFC模块的驱动（USART2），包含初始化、寻卡、刷卡响应等功能，刷到卡时触发彩虹流水灯动画。编译过程中的C90声明位置、未使用参数、const丢弃等警告已修复。
5. 整理了PID调参指南，当前参数KP=5.0, KI=0.016, KD=0.003，后续需根据实际跑车效果微调。
待验证：刷卡触发流水灯、直线前进1米精度、PID参数优化。
二、Hi386方面
这方面今天主要实现超声波测距功能，其中在代码的烧录上出了点问题浪费了不少时间，串口返回老是time out，但总体来说难度比STM32控制小车走直线要简单，在基本的代码复现上稍微调衡一下就完成了。
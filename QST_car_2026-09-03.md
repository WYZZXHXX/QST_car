2026-09-03 工作日志

一、超声波 + 红外自动避障小车（14.0_Hcsr04_Avoid）

1. 完成超声波自动避障整车逻辑：HC-SR04（TRIG=GPIO7、ECHO=GPIO8）测距，前方距离 < 20cm 时判定为障碍，执行「后退 1 秒 → 舵机左右探测 → 朝空旷方向原地转向 → 直行」。舵机用 SG90（GPIO2），测距函数沿用 4.0 的 `GetDistance()` 并增加超时保护（50000 次轮询后返回 0），返回 0 视为超量程/无障碍。

2. 智能避障决策：后退后舵机分别转到 0°（左，500us）与 180°（右，2500us）各测一次距离，`d == 0` 或 `d > 20cm` 记为「空旷」；两边都空旷时优先左转，仅单边空旷则转向该侧，两边都有障碍则继续后退。转向采用原地转 `car_spin_left/right`（左右轮反向旋转，绕中心转 90°，时长约 1.5s）。

3. 红外避障：TCRT5000 左右红外对管（左=GPIO13、右=GPIO14）检测黑线/桌面边缘，单边压线时「后退 1 秒 → 弧线转向」避开（左压线右转、右压线左转），两边同时压线则后退后再右转。红外触发极性经调试确定为高电平（`v == VALUE1`）代表压线。

4. 电机控制复用 13.0 的 GPIO11 软件模拟串口（bit-bang，9600）下发 6 字节帧 `0xFC + A_dir + motorA + B_dir + motorB + 0xFD`，发帧期间 `hi_int_lock`/`hi_int_restore` 关中断保证时序；提供前进/后退/弧线左转/弧线右转/原地左转/原地右转/停止七种运动函数。

二、调试中解决的问题

1. 编译报错 `car_left defined but not used`（工程开启 `-Werror=unused-function`）：因单边压线改用原地转后 `car_left` 不再被调用，删除该未使用函数解决。

2. 红外极性判断：初版按低电平=压线，实测白桌面始终显示 L1 R1（误判），确认本模块 TCRT5000 为「高电平=压线」，将 `TcrtRead` 中的比较由 `VALUE0` 改为 `VALUE1` 后恢复正常。

3. 转向幅度不足：单边压线从弧线小转改为原地转，并调整 `TURN_90`（原地转 90°，900ms→1500ms）、`TURN_DELAY`（弧线转，600ms→1000ms）。

4. 超声波 `d` 恒为 0：代码逻辑、GPIO 初始化（含 GPIO7/8 复用与 `GpioInit`）、超时保护均核对无误，指向硬件链路（HC-SR04 需 5V 供电、ECHO 5V 电平到 3.3V GPIO 需分压、TRIG/ECHO 接线与共地）。已加过临时上电自检打印 ECHO 空闲电平与触发回波，后按需求回退。

5. 多次遇到 printf 中 `\r\n` 被文本处理工具转义破坏为真实换行导致字符串断裂，已通过精确字节替换逐一修复。

三、阶段小结

今日在打通电机链路与蓝牙遥控的基础上，落地了 `14.0_Hcsr04_Avoid` 超声波+红外自动避障整车应用（含舵机左右探测的智能转向决策）。调试中解决了未使用函数编译报错、红外触发极性、转向幅度、printf 字符串转义破坏等问题。

待验证：超声波避障实际距离测量（d 恒 0 指向硬件链路，需确认 HC-SR04 供电与 ECHO 电平）。

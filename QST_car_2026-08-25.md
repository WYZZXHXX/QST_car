2026.8.25-工作日志

1.了解了串口相关知识，使用STM32和CH340进行了跑马灯代码的实现（已给代码），并完成对该实验的复现和拓展，包括实现小车全身灯光渐变流动效果以及串口输入HELLO和BYE来控制灯光亮灭的功能，其他发光效果也进行相关尝试。
该过程中在安装CH340和ST-Link的驱动程序上花费较多时间，最后利用大模型（DeepSeek-V4 Pro）解决了问题。
在运行代码过程中出现的问题，例如Load代码后无反应等，在AI大模型的帮助下，并且类比了大一春季学期的电子设计课程内容，能够很快地解决
整理的一些相关内容
（1）WS2812 的通信原理
  WS2812 是「单线协议」，没有时钟线，靠数据线上一段时间内高/低电平的宽度比例来区分 0 和 1：
  - 发 0：高电平约 0.4µs + 低电平约 0.85µs（高短低长）
  - 发 1：高电平约 0.85µs + 低电平约 0.4µs（高长低短）
  - 复位：数据线拉低超过 ~50µs（代码里用了 66µs），灯珠就知道「一帧结束」
 （2）串口控制逻辑
   main.c 里通过串口命令控制全亮/全灭：
   if(USART_RX_STA==1) {          // 收到 "HELLO"
      for(i=1; i<=led_num; i++) { L_ws2812_rgb(i, WS_WHITE); R_ws2812_rgb(i, WS_WHITE); }
      L_ws2812_refresh(led_num); R_ws2812_refresh(led_num);
      USART_RX_STA=0;}
  else if(USART_RX_STA==2) {     // 收到 "BYE" → 全灭（写 WS_DARK）}
    - USART_RX_STA 是串口接收状态标志：1 表示收到某条命令（HELLO）、2 表示另一条（BYE），处理完要清零，等下一次命令


2.搭建了Linux虚拟机环境，由于桥接模式下虚拟机无法正常连接网络，我通过询问大模型实现了NAT模式下在Windows系统上SSH
到虚拟机的NAT IP，并在VScode中实现SSH远程连接


3.在搭建Linux环境过程中学习了Linux系统的终端的相关知识，了解并练习了一些基础的指令，并在课下拓展了一些常用命令如：
     命令    │         作用                  │            示例             │
  │ pwd   │ 显示当前目录              │ pwd                         │
  │ ls       │ 列出文件                     │ ls -la（含隐藏+详情）       │
  │ cd      │ 切换目录                    │ cd /etc                     │
  │ mkdir │ 建目录                       │ mkdir -p a/b/c              │
  │ touch │ 新建空文件                 │ touch file.txt              │
  │ cp      │ 复制                           │ cp -r 源 目标               │
  │ mv     │ 移动/重命名                │ mv a.txt b.txt              │
  │ rm      │ 删除                           │ rm -rf 目录（⚠️危险，慎用） │
  │ cat      │ 查看文件全部内容        │ cat file.txt                │
  │ less     │ 分页查看（可上下翻）  │ less file.txt  
















































